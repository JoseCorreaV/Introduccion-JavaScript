// Saber si una variable es falsa o true

False
Boolean(0); 
Boolean(null);
Boolean(NaN);
Boolean(undefined); 
Boolean(False);
Boolean("");

// -------------------

True
Boolean(1);
Boolean(" ");
Boolean("a");
Boolean([]);
Boolean({});
Boolean(function(){});
Boolean(true);

